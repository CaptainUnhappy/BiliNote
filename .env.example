# ==================== 端口配置 ====================
BACKEND_PORT=8483
FRONTEND_PORT=3015
BACKEND_HOST=0.0.0.0
APP_PORT=3015  # Docker 部署时对外暴露的端口

# ==================== 前端配置 ====================
# 开发环境使用
VITE_API_BASE_URL=http://127.0.0.1:8483
VITE_SCREENSHOT_BASE_URL=http://127.0.0.1:8483/static/screenshots
VITE_FRONTEND_PORT=3015

# 注意：Docker 部署时，前端会自动使用 /api 路径通过 nginx 反向代理
# 无需修改 VITE_API_BASE_URL，Dockerfile 构建时已设置为空

# ==================== 后端配置 ====================
ENV=production
STATIC=/static
OUT_DIR=./static/screenshots
NOTE_OUTPUT_DIR=note_results
IMAGE_BASE_URL=/static/screenshots

# 数据目录 - 数据库、配置等都存储在此目录
# Docker 部署时此目录会被挂载为命名卷，实现数据持久化和用户共享
DATA_DIR=data
# FFMPEG 配置
FFMPEG_BIN_PATH=

# transcriber 相关配置
TRANSCRIBER_TYPE=fast-whisper # fast-whisper/bcut/kuaishou/mlx-whisper(仅Apple平台)/groq
WHISPER_MODEL_SIZE=base

GROQ_TRANSCRIBER_MODEL=whisper-large-v3-turbo # groq提供的faster-whisper 默认为 whisper-large-v3-turbo

# ==================== 代理配置 ====================
# LLM_PROXY: 仅用于 LLM API 请求（Google Gemini、OpenAI 等），不影响视频下载
# 推荐使用此配置，避免代理影响 B站/YouTube 视频下载
# LLM_PROXY=http://172.17.0.1:7890

# 全局代理配置 (不推荐，会影响所有网络请求包括视频下载)
# HTTP_PROXY=http://172.17.0.1:7890
# HTTPS_PROXY=http://172.17.0.1:7890
# NO_PROXY=localhost,127.0.0.1,backend,frontend,nginx
